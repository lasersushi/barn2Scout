import 'package:flutter/material.dart';

/// Placeholder — replaced in the "Match schedule / scouter assignment" task.
class Barn2SchedulePage extends StatelessWidget {
  const Barn2SchedulePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Barn 2 Scout — Schedule')),
      body: const Center(child: Text('Match schedule — coming next')),
    );
  }
}
